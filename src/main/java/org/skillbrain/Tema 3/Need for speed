class Main {
  public static void main(String[] args) {
    System.out.println("Hello world!");
  }
}
public class NeedforSpeed {
    public boolean batteryDrained() {
        int speed = 5;
        int batteryDrain = 2;
        NeedForSpeed car = new NeedForSpeed(speed, batteryDrain);
        int i=0;
        int i>5;
        int i1=0;
        int i1>2;
        if (i>5 & i=0 & i1=0 & i1>2,i++)
        System.out.println(NeedforSpeed);{
            return true;
        }
        public int distanceDriven() {
            int distance = 800;
            RaceTrack raceTrack = new RaceTrack(distance);
           int i=0;
            int i1=800;
            if (i=0 & i1=800)
                System.out.println(raceTrack(distance));{
                    return raceTrack distance;
            }
            public void drive() {
                int speed = 5;
                int batteryDrain = 2;
                NeedForSpeed car = new NeedForSpeed(speed, batteryDrain);
                car.drive();
                int i=0;
                int i2=5;
                int i3=2;
                if (i=0& i2=5& i3=2)
                    int i2-i3;
                    System.out.println(car.distanceDriven);{
                        return i2-i3;
                }

                public static boolean NeedForSpeed nitro() {
                    throw new NeedForSpeed nitro= NeedForSpeed nitro;
                     int car.batteryDrained;
                    int speed = 5;
                    int batteryDrain = 2;
                    NeedForSpeed car = new NeedForSpeed(speed, batteryDrain);
                    car.drive();
                    int i=0;
                    int i2=5;
                    int i3=2;
                    if (i=0& i2=5& i3=2)
                        int i2-i3;
                    System.out.println(car.batteryDrained);{
                        return  false;
                    }
                    public boolean carCanFinish(NeedForSpeed car) {
                        throw new carCanFinish=carCanFinish;
                        int speed = 5;
                        int batteryDrain = 2;
                        NeedForSpeed car = new NeedForSpeed(speed, batteryDrain);

                        int distance = 100;
                        RaceTrack race = new RaceTrack(distance);
                        int i=0;
                        int i2=5;
                        int i3=2;
                        if (i=0& i2=5& i3=2)
                            int i2-i3;
                        System.out.println(NeedforSpeed);{
                            return  true;
                        }
                            throw new disttance=distance;
                        a=100;
                        if (i=0&a=100)
                            System.out.println(race.carCanFinish(car));{
                                return  true;
                        }


        }
    }
}